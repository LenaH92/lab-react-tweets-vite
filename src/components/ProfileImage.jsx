function profilePicture({profileIMG}){ //passing as an argument what the teewt will put
    return (<img
        src={image} 
        className="profile"
        alt="profile"
      />)
}

export default profilePicture;